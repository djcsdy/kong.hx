#if flash9

package kong;

class EventType {
    /** The type of event dispatched when the player logs in, and thus converts from a guest to a registered user. */
    public static inline var LOGIN = "login";
}

#end